import { getFilesWithLocales } from '../script-utils.mjs';
import { copy } from '../create-cohort-project.mjs';
describe('create-cohort-project script', () => {
  describe('copy function', () => {
    it ('should rename all files that have the selected locale when locale is not default', () => {

    });

    it('should unlink all files after renaming that remain without default locale', () => {

    });

    it('should return ...', () => {

    })
  });
  
  describe('getFilesWithLocale', () => {
    it('should return all files with .pt.md when locale is pt', () => {

    });
  
    it('should return all files with supported locales', () => {

    });
  })
})
